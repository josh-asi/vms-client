import axios from 'axios';
import { server } from '../helpers';
import { AnyAction, Dispatch } from 'redux';

const endpoint = `${server}/vehicle`;

export const getVehiclesRequest = () => async (
  dispatch: Dispatch<AnyAction>
) => {
  try {
  } catch {}
};
